<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="GridBackgroundStyle" TargetType="Grid">
    <Setter Property="Background" Value="#1E1E1E" />
  </Style>
    
  <Style x:Key="SidebarBorderStyle" TargetType="Border">
    <Setter Property="Background" Value="#2E2E2E" />
    <Setter Property="BorderBrush" Value="#FFD7DCE3" />
    <Setter Property="BorderThickness" Value="0,0,1,0" />
  </Style>

  <Style x:Key="CanvasBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="#FFE3E6EA" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style x:Key="SidebarActionButtonStyle" TargetType="Button">
    <Setter Property="Height" Value="30" />
    <Setter Property="Margin" Value="10,10,10,10" />
    <Setter Property="Padding" Value="12,4" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="#FF1F2933" />
    <Setter Property="Background" Value="#FFE7ECF2" />
    <Setter Property="BorderBrush" Value="#FFCBD5E1" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Border"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="10">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Border" Property="Background" Value="#FFDCE6F3" />
              <Setter TargetName="Border" Property="BorderBrush" Value="#FFB6C3D6" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Border" Property="Background" Value="#FFCAD7E8" />
              <Setter TargetName="Border" Property="BorderBrush" Value="#FFA8B7CC" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Border" Property="Background" Value="#FFF1F5F9" />
              <Setter TargetName="Border" Property="BorderBrush" Value="#FFE2E8F0" />
              <Setter Property="Foreground" Value="#FF94A3B8" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SectionHeaderTextStyle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="16" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Margin" Value="0,0,0,12" />
  </Style>

  <Style x:Key="FlowBlockListStyle" TargetType="ListBox">
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <Style x:Key="DesignCanvasStyle" TargetType="Canvas">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="AllowDrop" Value="True" />
  </Style>
</ResourceDictionary>
