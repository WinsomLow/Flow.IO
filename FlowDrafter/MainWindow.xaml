<Window x:Class="FlowDrafter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowDrafter"
        xmlns:controls="clr-namespace:FlowDrafter.Controls"
        mc:Ignorable="d"
        Title="Flow.IO" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0"
                Background="#FFF2F4F7"
                BorderBrush="#FFD7DCE3"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="12">
                <TextBlock Text="Flow Blocks"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />
                <ListBox x:Name="FlowBlockList"
                         BorderThickness="0"
                         Background="Transparent"
                         PreviewMouseMove="FlowBlockList_OnPreviewMouseMove">
                    <ListBoxItem Content="Start" Padding="8" Margin="0,0,0,6" />
                    <ListBoxItem Content="Process" Padding="8" Margin="0,0,0,6" />
                    <ListBoxItem Content="Decision" Padding="8" Margin="0,0,0,6" />
                    <ListBoxItem Content="End" Padding="8" />
                </ListBox>
            </StackPanel>
        </Border>
        <Border Grid.Column="1"
                Background="White"
                BorderBrush="#FFE3E6EA"
                BorderThickness="1">
            <Canvas x:Name="DesignCanvas"
                    Background="Transparent"
                    AllowDrop="True"
                    DragOver="DesignCanvas_OnDragOver"
                    Drop="DesignCanvas_OnDrop" />
        </Border>
    </Grid>
</Window>
